<?php

namespace Rotaz\Hex\Event;

/**
 * Interface that defines the structure for inbound events received by the service.
 * This interface extends the base EventInterface, ensuring that all inbound events
 * conform to a standardized structure and behavior.
 */
interface InboundEventInterface extends EventInterface
{
    // Para eventos recebidos pelo serviço
}